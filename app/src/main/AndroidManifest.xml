<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="acy.iut.theviborationuniversity" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->

    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="com.package.name.LoginActivity"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name"
            android:name=".MainMenu">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".LesChats">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".LesProfiles">

        </activity>

        <activity android:label="@string/app_name"
            android:name=".LesGenres">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".ProfileEtudiant">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".ProfileAutreEtudiant">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".ModifProfil">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".InsertTrack">

        </activity>
        <activity android:label="@string/app_name"
            android:name=".DeleteTrack">

        </activity>


    </application>

</manifest>
